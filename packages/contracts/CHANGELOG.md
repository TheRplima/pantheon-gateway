# Changelog

## 0.1.0

- Initial `v1` event envelope schema.
- Initial `v1` lifecycle event schemas:
  - `agent.create.requested`
  - `agent.create.completed`
  - `agent.registered`
  - `agent.activated`
- Added example payloads for all `v1` events.
